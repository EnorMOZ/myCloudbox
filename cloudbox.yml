---
- hosts: localhost
  become: True
  vars_files:
    - settings.yml
    - "/home/seed/.settings.yml"
    - bootstrap.yml
  roles:
    - role: "roles/common"
      tags: common
    - role: "roles/cloudbox"
      tags: cloudbox


